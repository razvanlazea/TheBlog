<%= render(partial: "header") %>
<h1 id = "indexArticles">Articles</h1>

<div class = "articles">
	<% @posts.each do |p| %>
	<%= render(partial: "article", object: p) %>
	<% end %>
	<%= will_paginate @posts %>
	
</div>